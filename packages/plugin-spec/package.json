{
	"name": "@agenticflowx/plugin-spec",
	"description": "AFX spec engine — models, config, spec parser, tools (platform-agnostic)",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"lint": "eslint src --ext=ts --max-warnings=0",
		"check-types": "tsc --noEmit",
		"test": "vitest run --passWithNoTests",
		"clean": "rimraf .turbo"
	},
	"dependencies": {
		"@agenticflowx/types": "workspace:^",
		"gray-matter": "^4.0.3",
		"yaml": "^2.7.0"
	},
	"devDependencies": {
		"@agenticflowx/config-eslint": "workspace:^",
		"@agenticflowx/config-typescript": "workspace:^",
		"@types/node": "^24.1.0",
		"vitest": "^3.2.3"
	}
}
